# ── API Connection ────────────────────────────────────────────────────────
# URL for the Go backend API. Used by the browser (client-side routing)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Internal URL used during server-side rendering (SSR) if applicable
INTERNAL_API_URL=http://localhost:8080
